# Check-in log 📓

> One short line per contact so the butler (and Kay) can see the trend without
> re-reading everything. Newest at the bottom. Format:
> `DATE TIME | type | mood | offered → did | note`

2026-07-07 setup | init | shame→motivated | built butler mechanism → n/a | Kay asked for an active, therapy-driven nudging system instead of passive files.
